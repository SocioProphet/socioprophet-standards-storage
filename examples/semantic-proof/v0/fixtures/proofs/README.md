# Semantic-proof proof-family fixtures

This follow-on fixture pack widens the initial semantic-proof seed with:

- exclusion proof examples
- append-only consistency proof examples
- snapshot-diff proof examples
- invalid proof examples mapped to explicit verifier failure classes

These fixtures are intentionally compact. Their job is to make the proof-family widening reviewable and test-oriented, not to be the final exhaustive corpus.
